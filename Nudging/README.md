Guide to the nudging script

The fields are nudged without an adiabatic temperature boundary condition (zero vertical heat-flux)

tempvel_nudge.c
    alpha - inverse time-scale of nudging term
    Parameters same as temppart_dump.c

mympi2.h 
    Nprtcl needs to be the same
